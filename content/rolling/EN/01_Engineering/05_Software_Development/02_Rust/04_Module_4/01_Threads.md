@title: Threads: std::thread
@icon: 🧵
@description: spawn, join, channels, Send/Sync.
@order: 1

# Threads: std::thread

`thread::spawn` requires `'static`.

@quiz: spawn closure bound?
@option: Copy
@correct: 'static
@option: Debug
