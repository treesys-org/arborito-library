@title: Variables: let, mut, shadowing
@icon: 📦
@description: Immutability by default, inference.
@order: 2

# Variables: let, mut, shadowing

`let` is immutable; `let mut` allows mutation. **Shadowing** can change types.

@quiz: How do you mark a variable mutable?
@option: volatile
@correct: let mut
@option: mut let
